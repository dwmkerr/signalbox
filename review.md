I notice that on the mobile app when the QR code fails to connect, it says must be on same wifi - but i'm on the same wifi (hotel) and it doesn't connect (as expected) - is there a better error?

I run 'signalbox --version' it says 0.1.5 as expected, but that's also the deployed version so it doesn't really help me see if it is the local vesrion - what do you think?

Seeding demo eessions is great;  should we have a way to tag them so we can tag with `#demo`? How would we show this? can you spec it? pill in the box? Ideas?

Dockerfile for hub - should this specify a USER so that we are non root?

I had to st the fly secret _before_ deploy otherwise it failed cause deploy died ten times
